import {
  globalStyles,
  fonts,
  breakpoints,
  breakpointKeys,
} from './globalStyles'
import { colors } from './colors'

const theme = { colors }

export { globalStyles, fonts, breakpoints, breakpointKeys, colors, theme }
